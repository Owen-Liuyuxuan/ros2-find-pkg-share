{
  "name": "ros2-find-pkg-share",
  "displayName": "ROS2 find-pkg-share Navigation",
  "description": "Ctrl+Click on $(find-pkg-share pkg)/path in ROS 2 launch XML files",
  "version": "0.1.0",
  "publisher": "Owen-Liuyuxuan",
  "engines": {
    "vscode": "^1.85.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onLanguage:xml"
  ],
  "main": "./out/extension.js",
  "contributes": {},
  "scripts": {
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "vscode:prepublish": "npm run compile"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/vscode": "^1.85.0",
    "typescript": "^5.3.0"
  }
}
